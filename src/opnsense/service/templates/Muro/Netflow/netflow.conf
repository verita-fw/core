#
# Automatic generated configuration for netflow.
# Do not edit this file manually.
#
{% from 'Muro/Macros/interface.macro' import physical_interface %}
{%
  if helpers.exists('Muro.Netflow.capture.interfaces')
  and
  Muro.Netflow.capture.interfaces.strip()
  and
  helpers.exists('Muro.Netflow.capture.targets')
  and
  Muro.Netflow.capture.targets.strip()
  %}
netflow_interfaces="{% for interface in Muro.Netflow.capture.interfaces.split(',')
%}{{
    physical_interface(interface)
}} {%
  endfor%}"
netflow_egress_only="{% if Muro.Netflow.capture.egress_only %}{% for interface in Muro.Netflow.capture.egress_only.split(',')
%}{{
    physical_interface(interface)
}} {%
  endfor%} {%endif%}"
netflow_version="{%if Muro.Netflow.capture.version == 'v9' %}9{% else %}5{%endif%}"
netflow_int_destination="127.0.0.1:2055"
netflow_destinations="{{Muro.Netflow.capture.targets.replace(',', ' ')}}"
netflow_active_timeout={{Muro.Netflow.activeTimeout|default(1800)}}
netflow_inactive_timeout={{Muro.Netflow.inactiveTimeout|default(15)}}
{% endif %}
